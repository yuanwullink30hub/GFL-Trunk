/**
 * The geometry-reference images (Corpus Manifest spine: "Geometry reference — wheel positions,
 * edge types, opposition/shadow map"). They live in prompts/images and are attached to every
 * report call by services/aiProviders.js; the manifest lists them so the slice is complete on paper.
 */
module.exports = Object.freeze(['Cells within Cells png.png', 'Deltawerken png.png', 'TNM wheel PNG.png']);
