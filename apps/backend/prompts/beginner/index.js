/**
 * Beginner / Level 1 — Prompt Builder (Placeholder)
 *
 * To be implemented when the Beginner assessment tier is ready.
 */

function buildSystemPrompt(/* data */) {
  return 'Beginner prompt — not yet implemented.';
}

function buildUserMessage(/* data */) {
  return 'Beginner prompt — not yet implemented.';
}

module.exports = { buildSystemPrompt, buildUserMessage };
