/**
 * Intermediate / Level 2 — Prompt Builder (Placeholder)
 *
 * To be implemented when the Intermediate assessment tier is ready.
 */

function buildSystemPrompt(/* data */) {
  return 'Intermediate prompt — not yet implemented.';
}

function buildUserMessage(/* data */) {
  return 'Intermediate prompt — not yet implemented.';
}

module.exports = { buildSystemPrompt, buildUserMessage };
