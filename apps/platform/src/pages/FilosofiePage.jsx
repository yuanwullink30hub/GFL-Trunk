import React, { memo } from 'react';

/**
 * FilosofiePage — intentionally empty for now.
 *
 * The interactive "playbox" that used to live here has been moved to
 * src/hypercube-content/filosofie/ and will be re-mounted later, once we
 * start inhabiting the hypercube with content. The map section is kept so
 * navigation still resolves; it simply renders nothing in the meantime.
 */
const FilosofiePage = memo(() => null);

FilosofiePage.displayName = 'FilosofiePage';

export default FilosofiePage;
