// Headless assessment data + scoring (no React, no bundled assets).
// archetypeImages exposes /images/... public-path strings (served by the
// consuming app's public/ dir), not bundled imports.
export * from './data/index.js';
